{
  "routes": [
    {
      "route": "/api/*",
      "allowedRoles": [ "authenticated" ]
    }
  ],
  "navigationFallback": {
    "rewrite": "index.html",
    "exclude": ["*.{js,css,png,jpg,gif,ico}"]
  }
}
